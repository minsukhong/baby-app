# 아기 케어 앱 - 코드 가이드라인
> 목적: 코드 작업 전 반드시 이 문서를 참고. 구조 확인 → 승인 → 코드 순서 준수.
> 규칙: 어떤 AI든 이 문서를 먼저 읽고, 구조 변경 시 반드시 개발자 승인 후 진행.

---

## 전체 개발 단계 (로드맵)

| 단계 | 기능 | 상태 |
|------|------|------|
| 1단계 | 개발 환경 세팅 (Docker, MSA, Dev Container) | ✅ 완료 |
| 2단계 | 로그인 / 인증 (일반 + Google + 카카오) | ✅ 완료 (일반 로그인) |
| 3단계 | 아기 프로필 등록 | ⬜ 대기 |
| 4단계 | 수유 기록 | ⬜ 대기 |
| 5단계 | 수면 기록 | ⬜ 대기 |
| 6단계 | 성장 기록 (몸무게/키) | ⬜ 대기 |
| 7단계 | 대시보드 (통계/차트) | ⬜ 대기 |
| 8단계 | 알림 기능 | ⬜ 대기 |
| 9단계 | 배포 (Vercel + Railway + Supabase) | ✅ 완료 |

---

## 현재 진행: 3단계 대기 중 (2단계 완료)

### 브랜치
```
master   ← 현재 최신 (배포 기준)
└── develop
    └── feature/auth-login   ← 2단계 완료, master에 병합 예정
```

### 2단계 완료 내역 (2026-03-16)

#### DB
| 파일 | 작업 | 상태 |
|------|------|------|
| `db/init.sql` | users 테이블 추가 | ✅ |
| Supabase | users 테이블 생성 + admin 시드 (API로 직접 실행) | ✅ |

#### Backend
| 파일 | 작업 | 상태 |
|------|------|------|
| `backend/requirements.txt` | 패키지 추가 (bcrypt, JWT, OAuth) | ✅ |
| `backend/database.py` | DB 연결 설정 | ✅ |
| `backend/models/user.py` | User 모델 | ✅ |
| `backend/schemas/user.py` | 요청/응답 스키마 | ✅ |
| `backend/core/security.py` | JWT, 비밀번호 암호화 | ✅ |
| `backend/routers/auth.py` | 로그인 API | ✅ |
| `backend/main.py` | 라우터 등록 + /db-test 엔드포인트 | ✅ |

#### Frontend
| 파일 | 작업 | 상태 |
|------|------|------|
| `frontend/lib/auth.ts` | API 호출, 토큰 관리 | ✅ |
| `frontend/components/auth/LoginForm.tsx` | 로그인 폼 컴포넌트 (shadcn/ui) | ✅ |
| `frontend/app/login/page.tsx` | 로그인 화면 UI | ✅ |
| `frontend/app/dashboard/page.tsx` | 로그인 후 메인화면 (빈 껍데기) | ✅ |
| `frontend/app/page.tsx` | 루트 → 로그인 리다이렉트 | ✅ |

---

## 배포 현황 (라이브)

| 서비스 | URL | 상태 |
|--------|-----|------|
| **Frontend** | https://baby-app-delta.vercel.app | ✅ 운영 중 |
| **Backend** | https://baby-app-production.up.railway.app | ✅ 운영 중 |
| **DB** | Supabase (ap-southeast-2 / Sydney) | ✅ 운영 중 |

### Railway 배포 정보
- **프로젝트명**: genuine-creativity
- **프로젝트 ID**: `0c4f2f41-1e4c-437a-b38d-07c01f17c29e`
- **서비스 ID**: `1567166a-c2da-4844-aedf-104bb63f407b`
- **환경 ID**: `158370c4-cfb1-4a34-afb5-7fbcba5c391d`

### Vercel 배포 정보
- **프로젝트 ID**: `prj_eewBhBOizB33cJJifL6FjYrkVgB2`

### Supabase 정보
- **프로젝트 REF**: `rgcfofduvijaovwijszy`
- **리전**: ap-southeast-2 (Sydney)
- **연결 방식**: IPv4 Pooler (Session Mode)
  - Host: `aws-1-ap-southeast-2.pooler.supabase.com:5432`
  - User: `postgres.rgcfofduvijaovwijszy`
- ⚠️ **직접 연결 URL** (`db.rgcfofduvijaovwijszy.supabase.co`) = IPv6 전용 → Railway에서 불가
- ✅ **Pooler URL** = IPv4 지원 → Railway에서 사용 중

### Railway 환경변수 (현재 설정값)
| 변수 | 값 |
|------|----|
| `DATABASE_URL` | `postgresql://postgres.rgcfofduvijaovwijszy:[PW]@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres` |
| `JWT_SECRET` | `baby-app-secret-key-2026-very-secure` |
| `ALLOWED_ORIGINS` | `http://localhost:3000,https://baby-app-delta.vercel.app` |
| `PORT` | `8000` |

### Vercel 환경변수
| 변수 | 값 |
|------|----|
| `NEXT_PUBLIC_API_URL` | `https://baby-app-production.up.railway.app` |

---

## CLI 제어 (scripts/deploy_control.py)

배포 서비스 3개를 Python 스크립트로 직접 제어 가능.

```bash
# 전체 상태 확인
python scripts/deploy_control.py status

# Railway 재배포
python scripts/deploy_control.py redeploy

# Railway 환경변수 목록
python scripts/deploy_control.py env

# Supabase SQL 실행
python scripts/deploy_control.py db "SELECT * FROM users;"

# 로그인 동작 테스트
python scripts/deploy_control.py login_test
```

토큰은 `.env.deploy` 파일에 저장 (`.gitignore`에 포함, GitHub 비공개).

---

## 전체 파일 구조 (현재 기준)

```
baby-app/
├── .devcontainer/
│   ├── devcontainer.json
│   ├── docker-compose.yml
│   └── Dockerfile
├── frontend/                        ← Next.js 14
│   ├── app/
│   │   ├── page.tsx                 ✅ 루트 → /login 리다이렉트
│   │   ├── layout.tsx               ✅ Inter 폰트, globals.css
│   │   ├── globals.css              ✅ Tailwind v3 + shadcn 색상 토큰
│   │   ├── login/
│   │   │   └── page.tsx             ✅ 로그인 화면
│   │   └── dashboard/
│   │       └── page.tsx             ✅ 대시보드 (빈 껍데기)
│   ├── components/
│   │   ├── auth/
│   │   │   └── LoginForm.tsx        ✅ shadcn/ui 기반 로그인 폼
│   │   └── ui/                      ✅ shadcn/ui 컴포넌트들
│   ├── lib/
│   │   ├── auth.ts                  ✅ API 호출, 토큰 관리
│   │   └── utils.ts                 ✅ cn() 유틸
│   ├── tailwind.config.ts           ✅ shadcn 색상 토큰 포함
│   └── package.json                 ✅ Next.js 14.2.29
├── backend/                         ← FastAPI
│   ├── main.py                      ✅ CORS, 라우터, /health, /db-test
│   ├── requirements.txt             ✅
│   ├── database.py                  ✅ SQLAlchemy + Pooler URL
│   ├── Procfile                     ✅ Railway 실행 명령
│   ├── models/
│   │   └── user.py                  ✅ User 모델
│   ├── schemas/
│   │   └── user.py                  ✅ LoginRequest, TokenResponse
│   ├── core/
│   │   └── security.py              ✅ JWT, bcrypt
│   └── routers/
│       └── auth.py                  ✅ /auth/login, /auth/me
├── db/
│   └── init.sql                     ✅ users 테이블 + admin 시드
├── scripts/
│   └── deploy_control.py            ✅ 배포 제어 스크립트
├── .env.deploy                      🔒 API 토큰 (gitignore)
├── ARCHITECTURE.md                  ✅
├── default_setting.md               ✅
├── 코드_가이드라인.md               ✅ 현재 파일
└── .gitignore                       ✅
```

---

## 보안 지침 (모든 코드에 적용)

### 절대 GitHub에 올리면 안 되는 것
| 항목 | 저장 위치 | 비고 |
|------|----------|------|
| Google OAuth Client ID/Secret | `.env` | `.gitignore`에 포함 |
| Kakao REST API Key | `.env` | `.gitignore`에 포함 |
| JWT Secret Key | `.env` | `.gitignore`에 포함 |
| DB 비밀번호 | `.env` | `.gitignore`에 포함 |
| 배포 API 토큰 | `.env.deploy` | `.gitignore`에 포함 |
| 관리자 계정 정보 | DB 시드 (암호화) | 코드에 평문 금지 |

### .env 파일 구조
```
# 절대 GitHub에 커밋하지 말 것
backend/.env         ← 백엔드 환경변수
frontend/.env.local  ← 프론트엔드 환경변수
.env.deploy          ← 배포 API 토큰 (Vercel/Railway/Supabase)
```

### 코드 보안 원칙
1. **비밀번호** → bcrypt로 해싱, DB에 평문 저장 절대 금지
2. **개인정보** → 전송 시 HTTPS, DB 저장 시 필요한 것만 최소 저장
3. **JWT 토큰** → 만료 시간 설정 필수 (access: 1h, refresh: 7d)
4. **API 키** → 코드에 하드코딩 금지, 반드시 환경변수로 참조
5. **SQL Injection** → ORM(SQLAlchemy) 사용, raw query 금지
6. **CORS** → 허용 도메인 명시적으로 지정
7. **소셜 로그인 키** → 서버사이드에서만 사용, 프론트에 노출 금지

---

## 배포 아키텍처 (확정)

| 서비스 | 역할 | 이유 |
|--------|------|------|
| **Vercel** | Frontend (Next.js) | Next.js 전용, CDN 최강, 무료 |
| **Railway** | Backend (FastAPI) | 무료 $5 크레딧, 간단한 배포 |
| **Supabase** | DB (PostgreSQL) | 자동 백업, 파일 저장, 소셜 로그인 내장 |

### 확장 전략
```
현재 ~ 1만 사용자  → Vercel + Railway + Supabase (무료)
10만 사용자 이상   → AWS/GCP 마이그레이션 고려 (MSA 구조라 부분 전환 가능)
```

### GitHub → 자동 배포 흐름
```
git push origin master
  ├── Vercel: frontend/ 변경 감지 → 자동 빌드 & 배포
  └── Railway: backend/ 변경 감지 → 자동 빌드 & 배포
```

---

## AI 작업 규칙

1. **코드 작업 전 반드시 이 파일 읽기**
2. **구조 변경 시 개발자 승인 후 진행**
3. **작업 완료된 파일은 상태를 ✅로 업데이트**
4. **새 파일 추가 시 이 문서 파일 구조에 반영**
5. **새 개발 단계 시작 시 "현재 진행" 섹션 업데이트**
6. **MSA 구조 (frontend/backend/db 분리) 절대 변경 금지**
7. **환경변수(.env) 파일 내용을 코드에 하드코딩 금지**
8. **민감 정보가 포함된 파일은 .gitignore 확인 후 작업**
9. **Railway DB 연결은 반드시 IPv4 Pooler URL 사용** (직접 연결 URL은 IPv6 전용)
10. **배포 제어는 scripts/deploy_control.py 활용**
