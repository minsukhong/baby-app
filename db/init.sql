-- baby-app 초기 데이터베이스 설정
-- 앱 기능이 확정되면 여기에 테이블을 추가합니다

-- 예시: 아기 정보 테이블 (추후 확정)
-- CREATE TABLE babies (
--   id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--   name VARCHAR(50) NOT NULL,
--   birth_date DATE NOT NULL,
--   created_at TIMESTAMP DEFAULT NOW()
-- );
